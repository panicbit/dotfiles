#!/bin/bash

#volume="$(ponymix get-volume)"
volume="$(midas volume get)"
msg="$(echo "$volume" | gdbar -l "Volume: $volume% " -fg '#fff' -bg '#444')"

echo "$msg"

if ponymix is-muted; then
    msg="$msg | ^fg(red)Muted"
fi

dzen-msg "$msg"
